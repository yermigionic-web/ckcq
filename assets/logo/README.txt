Place the final event logo here as:

  main-logo.png

Expected file:
  assets/logo/main-logo.png

If a source file such as CKCQ_logo.png exists in the repo root, copy or export it here.

Notes:
- Transparent background is ideal
- Chrome, black, neon lime, alien, UFO, stars
- The site already shows a text fallback if this file is missing
- Once this PNG exists and loads, the fallback hides automatically
- Do not replace the filename; the HTML looks for main-logo.png
